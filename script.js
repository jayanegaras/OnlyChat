// tst chats

/* document.addEventListener("DOMContentLoaded", () =>{
    const inputField = document.querySelector('#input');
    const button = document.querySelector('#output');
    const container = document.querySelector('#contain')
    button.addEventListener('click', ()=>{
        const chatInput = inputField.value;
        const chatBubble = document.createElement('div');
        const username = document.createElement('span')
        const chat = document.createElement('p');
        username.classList.add('username');
        chatBubble.classList.add('message') ;      
        chatBubble.classList.add('message-right');
        chat.textContent = chatInput;
        
        chatBubble.appendChild(username);
        chatBubble.appendChild(chat);

        container.appendChild(chatBubble);

    });
})

*/